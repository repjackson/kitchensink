template(name='nav')
    .ui.menu(href='/')
        // a#home_link.ui.header.icon.item(href='/' class="{{isActivePath '/'}}")
        //     img.logo(src='/favicon-32x32.png')
        // i.diamond.icon
        // |Facet
        // i.large.home.icon
        // .ui.category.search.two.wide.item
        //     .ui.transparent.inverted.icon.input
        //         input.search(type='text', placeholder='Search...')
        //         i.search.link.icon
        a.item(href='/docs' class="{{isActivePath '/docs'}}")
            i.file.outline.icon
            |Documents
        a.item(href='/people' class="{{isActivePath '/people'}}")
            i.users.icon
            |People
        // a.item(href='/exchange' class="{{isActivePath '/exchange'}}")
        //     i.exchange.icon
        //     |Exchange
        // a.item(href='/importers' class="{{isActivePath '/importers'}}")
        //     i.down.arrow.icon
        //     |Importers
        .right.menu
            if currentUser
                // #add_doc.ui.simple.icon.dropdown.item
                .ui.simple.icon.dropdown.item
                    i.plus.icon
                    // span.text Add
                    i.dropdown.icon
                    .menu
                        if selected_doc_tags
                            #new_from_selection.item
                                i.external.square.icon
                                |From Selection
                        #add_doc.item
                            i.file.outline.icon
                            |Blank
                        if currentUser.bookmarks
                            .header From Bookmarks
                            each currentUser.bookmarks
                                .item.add_from_bookmark
                                    each this
                                        .ui.label #{this}
                .ui.simple.icon.dropdown.item
                    i.bookmark.icon
                    // span.text Bookmarks
                    i.dropdown.icon
                    .menu
                        if selected_doc_tags
                            #bookmark_selection.ui.icon.circular.button
                                i.plus.icon
                                | Bookmark Current Selection
                        each currentUser.bookmarks
                            .item.select_bookmark
                                each this
                                    .ui.label #{this}
                a.ui.item(href='/messages' class="{{isActivePath '/messages'}}")
                    i.mail.outline.icon
                a.item(href='/profile' class="{{isActivePath '/profile'}}")
                    i.user.icon
                    | #{currentUser.username} [#{currentUser.points} pts]
            +atNavButton