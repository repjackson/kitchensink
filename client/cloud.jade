template(name='cloud')
    .ui.center.aligned.bottom.attached.segment
        // .ui.header Filter Docs
        if selectedTags
            #bookmarkSelection.ui.basic.button
                i.bookmark.icon
                |Bookmark Selection
            #clearTags.ui.primary.big.compact.circular.icon.button
                i.remove.icon
        if selectedTags
            each selectedTags
                a.ui.primary.big.compact.button.unselectTag
                    strong #{this}
        each globalTags
            .ui.compact.basic.button.selectTag(class=globalTagClass) #{this.name}
                small #{this.count}
        .ui.hidden.divider