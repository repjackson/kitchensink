template(name='person')
    .ui.fluid.card
        .content
            .header
                |#{username}
                each tagCloud
                    .ui.large.compact.button.userTag(class=userTagClass)
                        |#{this.name}
                        small #{this.count}
        unless isUser
            if currentUser
                .extra.content
                    if hasTagged
                        each myTags
                            .ui.labeled.basic.icon.button.removeMyTag
                                i.remove.icon
                                |#{this}
                        .ui.left.icon.input
                            i.tag.icon
                            input.addTag(type='text' placeholder='Add Tag')
                    else
                        .ui.basic.button.tagUser Tag User
                    // | Common tags with #{username} (Match Points: #{upVotedMatchCloud.totalCount})
                    // div
                    //     each upVotedMatchCloud.cloud
                    //         .ui.basic.label #{this.tag} #{this.min}