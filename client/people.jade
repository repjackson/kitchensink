
template(name='people')
    if currentUser
        +profile
    else
        .ui.large.basic.label
            +loginButtons
    +cloud
    .ui.container
        each people
            +person



template(name='person')
    .ui.segment
        h3.ui.header
            img.ui.small.circular.image(src=picture)
            |#{username}
        each cloud
            .ui.large.label.person_tag(class=person_tag_class) #{this}  
        if currentUser
            .ui.header Your Common Tags with #{username}
            each person_intersection
                .ui.label.person_tag(class=person_tag_class) #{this}
        .ui.header Tag Person
        .ui.input
            input#tag_person(type='text' placeholder="Tag #{username}" )